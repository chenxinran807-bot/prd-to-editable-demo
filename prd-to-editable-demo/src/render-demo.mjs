import { runtimeSource } from './templates/runtime.js';

function escapeHtml(value = '') {
  return String(value).replace(/[&<>"']/g, character => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
  })[character]);
}

function renderElement(element) {
  const attributes = `data-proto-key="${escapeHtml(element.key)}"${element.action ? ` data-action="${escapeHtml(JSON.stringify(element.action))}"` : ''}`;
  if (element.type === 'heading') return `<div class="content-line" ${attributes}>${escapeHtml(element.text)}</div>`;
  return `<button class="primary-action" type="button" ${attributes}>${escapeHtml(element.text)}</button>`;
}

const icon = name => `<svg class="icon" aria-hidden="true"><use href="#icon-${name}"></use></svg>`;

function renderComponent(name, model, page) {
  const objects = model.requirements?.businessObjects?.filter(Boolean) ?? [];
  const primary = escapeHtml(objects[0] ?? page.title);
  const evidence = model.demoContext?.originalPrd ?? '';
  const price = evidence.match(/[¥￥]\s?(\d+(?:\.\d{1,2})?)/)?.[1];
  if (name === 'top-bar') return `<header class="app-bar" data-component="top-bar"><button class="icon-button" type="button" aria-label="返回">${icon('back')}</button><strong>${escapeHtml(page.title)}</strong><button class="icon-button" type="button" aria-label="更多">${icon('more')}</button></header>`;
  if (name === 'search-bar') return `<div class="search" data-component="search-bar">${icon('search')}<input aria-label="搜索" value="${primary}" readonly><button type="button">搜索</button></div>`;
  if (name === 'filter-bar') return `<nav class="filters" data-component="filter-bar" aria-label="筛选条件"><button class="active" type="button">综合</button><button type="button">销量</button><button type="button">价格 ${icon('sort')}</button><button type="button">筛选 ${icon('filter')}</button></nav>`;
  if (name === 'product-card') return `<article class="product-card" data-component="product-card"><div class="product-visual" role="img" aria-label="${primary}商品视觉"><span>${primary.slice(0, 4)}</span></div><div class="product-info"><span class="commerce-tag">${escapeHtml(page.title)}</span><h3>${primary}</h3><p class="benefit">查看${primary}相关信息</p>${price ? `<div class="price"><small>¥</small>${escapeHtml(price)}</div>` : ''}</div></article>`;
  if (name === 'result-list') return `<div class="result-meta" data-component="result-list"><strong>为你找到相关结果</strong><span>按推荐排序</span></div>`;
  if (name === 'bottom-tabbar') return `<nav class="tabbar" data-component="bottom-tabbar" aria-label="主导航"><button class="active" type="button">${icon('home')}<span>首页</span></button><button type="button">${icon('category')}<span>分类</span></button><button type="button">${icon('cart')}<span>购物车</span></button><button type="button">${icon('user')}<span>我的</span></button></nav>`;
  if (name === 'dialog') return `<section class="dialog-card" data-component="dialog" role="dialog" aria-label="确认操作"><strong>请确认当前操作</strong><p>继续前请核对相关信息。</p></section>`;
  if (name === 'state-feedback') return `<div class="state-feedback state-${escapeHtml(page.state)}" data-component="state-feedback" role="status"><strong>${escapeHtml(page.title)}</strong><span>当前状态：${escapeHtml(page.state)}</span></div>`;
  if (name === 'price') return `<span data-component="price" hidden></span>`;
  if (name === 'commerce-tag') return `<span data-component="commerce-tag" hidden></span>`;
  if (name === 'button') return `<span data-component="button" hidden></span>`;
  return `<div data-component="${escapeHtml(name)}" hidden></div>`;
}

function renderPage(model, page) {
  const components = model.designCore?.components ?? [];
  const top = components.includes('top-bar') ? renderComponent('top-bar', model, page) : `<header class="app-bar" data-component="top-bar"><strong>${escapeHtml(page.title)}</strong><span class="state-pill">${escapeHtml(page.state)}</span></header>`;
  const middle = components.filter(name => !['top-bar', 'bottom-tabbar'].includes(name)).map(name => renderComponent(name, model, page)).join('\n');
  const bottom = components.includes('bottom-tabbar') ? renderComponent('bottom-tabbar', model, page) : '';
  return `<section class="proto-page" data-page-id="${escapeHtml(page.id)}" hidden>${top}<div class="page-scroll">${middle}<section class="business-content"><p class="eyebrow">${escapeHtml(model.product.name)}</p>${page.elements.map(renderElement).join('\n')}</section></div>${bottom}</section>`;
}

export function renderDemo(model) {
  const manifestJson = JSON.stringify(model).replaceAll('<', '\\u003c');
  const pages = model.pages.map(page => renderPage(model, page)).join('\n');
  const scenarioOptions = model.pages.map(page => `<option value="${escapeHtml(page.id)}">${escapeHtml(page.title)} · ${escapeHtml(page.state)}</option>`).join('');
  return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>${escapeHtml(model.product.name)} · 可编辑原型</title>
<style>
:root{font-family:Inter,"PingFang SC",sans-serif;color:#161823;background:#f5f6f9;--commerce:#FF003C;--line:#e8e8ed}*{box-sizing:border-box}body{margin:0;min-height:100vh}.review-bar{min-height:58px;background:#fff;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;padding:8px 20px;position:sticky;top:0;z-index:5}.brand{font-weight:700}.brand small{color:#8a8b91;font-weight:500}.mode-switch{display:flex;background:#f1f2f5;border-radius:9px;padding:3px}.mode-switch button,.toolbar button{border:0;background:transparent;padding:8px 12px;border-radius:7px;cursor:pointer}.toolbar{display:flex;align-items:center;gap:6px}.toolbar select{max-width:210px;padding:7px 9px;border:1px solid #d9dae0;border-radius:8px;background:#fff}body[data-mode="preview"] [data-mode="preview"],body[data-mode="edit"] [data-mode="edit"]{background:#fff;box-shadow:0 1px 4px #0002}.workspace{display:grid;grid-template-columns:1fr 340px;min-height:calc(100vh - 58px)}body[data-mode="preview"] .workspace{grid-template-columns:1fr}.canvas{padding:24px;display:grid;place-items:start center}.app-surface{width:min(430px,100%);min-height:760px;background:#fff;box-shadow:0 16px 48px #16182314;overflow:hidden}.proto-page{min-height:760px;position:relative;padding-bottom:66px}.app-bar{height:52px;padding:0 16px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #f0f0f2;background:#fff}.icon{width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:1.8}.icon-button{border:0;background:transparent;padding:8px}.state-pill{font-size:11px;color:#8a8b91}.page-scroll{padding-bottom:12px}.search{margin:12px 16px 8px;height:38px;border-radius:20px;background:#f2f3f5;display:flex;align-items:center;padding-left:12px;gap:8px}.search input{min-width:0;flex:1;border:0;outline:0;background:transparent;color:#161823}.search button{height:32px;border:0;border-radius:18px;background:var(--commerce);color:#fff;padding:0 15px;margin-right:3px}.filters{height:43px;display:flex;align-items:center;justify-content:space-around;border-bottom:1px solid #f2f2f4}.filters button{border:0;background:none;color:#5c5d65}.filters .active{color:#161823;font-weight:600}.filters .icon{width:13px;height:13px;vertical-align:-2px}.result-meta{display:flex;justify-content:space-between;padding:14px 16px 10px}.result-meta span{font-size:12px;color:#8a8b91}.product-card{margin:0 16px 12px;display:grid;grid-template-columns:116px 1fr;gap:12px}.product-visual{height:116px;border-radius:10px;background:linear-gradient(145deg,#ece7df,#d9c7b1);display:grid;place-items:center;color:#6e5d4c;font-weight:650}.product-info h3{font-size:15px;margin:7px 0 5px}.commerce-tag{font-size:10px;color:var(--commerce);border:1px solid #ffb3c5;border-radius:3px;padding:1px 3px}.benefit{font-size:12px;color:#8a8b91;margin:0 0 11px}.price{color:var(--commerce);font-weight:750;font-size:20px}.price small{font-size:12px}.price del{font-size:11px;color:#a9aab0;font-weight:400;margin-left:4px}.business-content{padding:12px 16px 24px;display:flex;flex-direction:column;gap:12px}.eyebrow{font-size:11px;color:#8a8b91;margin:0}.content-line{font-size:17px;font-weight:650;line-height:1.45}.primary-action{border:0;border-radius:9px;background:var(--commerce);color:#fff;padding:13px 18px;font-size:15px;font-weight:600;cursor:pointer}.primary-action:active{opacity:.76}.state-feedback,.dialog-card{margin:12px 16px;padding:18px;border-radius:12px;background:#f7f7f9;display:grid;gap:6px}.state-feedback span,.dialog-card p{font-size:13px;color:#8a8b91;margin:0}.tabbar{position:absolute;left:0;right:0;bottom:0;height:62px;display:flex;background:#fff;border-top:1px solid #eeeef1}.tabbar button{flex:1;border:0;background:none;display:grid;place-items:center;color:#8a8b91;font-size:10px;padding:7px}.tabbar .active{color:#161823}.tabbar span{display:block}.is-selected{outline:3px solid #ffb3c5!important;outline-offset:2px}.editor{background:#fff;border-left:1px solid #e1e2e6;padding:22px;overflow:auto}.editor h3{margin:18px 0 10px}.field{padding:10px;background:#f5f6f9;border-radius:8px;font-family:ui-monospace,monospace;word-break:break-all}.control{display:grid;gap:6px;margin:12px 0}.control input,.control select,.control textarea{width:100%;padding:9px;border:1px solid #d4d5da;border-radius:8px}.checks,.actions{display:flex;gap:8px;flex-wrap:wrap}.actions button{border:1px solid #d4d5da;background:#fff;border-radius:8px;padding:9px 11px;cursor:pointer}.actions .accent{background:var(--commerce);color:#fff;border-color:var(--commerce)}.footer-status{position:fixed;left:16px;bottom:14px;background:#161823;color:#fff;border-radius:9px;padding:8px 11px;font-size:12px;z-index:6}@media(max-width:760px){.review-bar{gap:7px;flex-wrap:wrap;padding:8px 12px}.toolbar{order:3;width:100%;overflow:auto}.workspace{grid-template-columns:1fr}.editor{position:fixed;left:0;right:0;bottom:0;max-height:48vh;z-index:7;border-top:1px solid #ddd}.canvas{padding:0}.app-surface{width:100%;min-height:calc(100vh - 110px);box-shadow:none}.proto-page{min-height:calc(100vh - 110px)}}
</style></head>
<body data-mode="preview"><svg width="0" height="0" aria-hidden="true"><defs><symbol id="icon-back" viewBox="0 0 24 24"><path d="M15 5l-7 7 7 7"/></symbol><symbol id="icon-more" viewBox="0 0 24 24"><circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/></symbol><symbol id="icon-search" viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="M16 16l5 5"/></symbol><symbol id="icon-sort" viewBox="0 0 24 24"><path d="M8 7h10M8 12h7M8 17h4"/></symbol><symbol id="icon-filter" viewBox="0 0 24 24"><path d="M4 6h16M7 12h10M10 18h4"/></symbol><symbol id="icon-home" viewBox="0 0 24 24"><path d="M4 11l8-7 8 7v9H4z"/></symbol><symbol id="icon-category" viewBox="0 0 24 24"><rect x="4" y="4" width="6" height="6"/><rect x="14" y="4" width="6" height="6"/><rect x="4" y="14" width="6" height="6"/><rect x="14" y="14" width="6" height="6"/></symbol><symbol id="icon-cart" viewBox="0 0 24 24"><path d="M3 5h2l2 11h11l2-8H6"/><circle cx="9" cy="20" r="1"/><circle cx="18" cy="20" r="1"/></symbol><symbol id="icon-user" viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M4 21c1-5 4-7 8-7s7 2 8 7"/></symbol></defs></svg><header class="review-bar"><div class="brand">${escapeHtml(model.product.name)} <small>评审原型</small></div><div class="toolbar"><label>场景 <select id="review-scenario">${scenarioOptions}</select></label><button id="undo-edit">撤销</button><button id="redo-edit">重做</button><button id="export-patches">导出修改</button></div><div class="mode-switch"><button data-mode="preview">预览</button><button data-mode="edit">编辑</button></div></header>
<main class="workspace"><div class="canvas"><div class="app-surface">${pages}</div></div><aside id="editor-panel" class="editor" hidden><h3>编辑元素</h3><div id="selected-key" class="field">尚未选择</div><label class="control">文案<input id="edit-text"></label><div class="checks"><label>文字色 <input id="edit-color" type="color" value="#161823"></label><label>背景色 <input id="edit-background" type="color" value="#FF003C"></label></div><label class="control">点击跳转<select id="edit-target"><option value="">无跳转</option></select></label><div class="checks"><label><input id="edit-hidden" type="checkbox"> 隐藏</label><label><input id="edit-disabled" type="checkbox"> 禁用</label></div><div class="actions"><button id="restore-element">恢复元素</button></div><hr><h3>让 Agent 修改</h3><label class="control">修改要求<textarea id="agent-request" rows="4" placeholder="例如：增加失败后的重试入口"></textarea></label><div class="actions"><button id="agent-submit" class="accent">保存修改任务</button><button id="export-comments">导出任务</button></div><p id="agent-status"></p></aside></main>
<div class="footer-status">当前页面：<span id="current-page-label"></span></div>
<script type="application/json" id="prototype-manifest">${manifestJson}</script><script>${runtimeSource}</script></body></html>`;
}
