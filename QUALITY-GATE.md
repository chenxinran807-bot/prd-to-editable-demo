# MVP 质量门槛

## 第一性原理

原型的价值不是“看起来像网页”，而是让评审者能够用最短路径回答：

1. 谁在什么场景下遇到什么问题？
2. 用户要完成什么任务？
3. 系统有哪些状态和分支？
4. 这个版本解决到什么程度？
5. 哪些内容是事实，哪些内容是推断？

如果原型无法支持以上判断，即使页面视觉完整，也不算通过。

## 对抗性验收清单

- [x] 页面不是由标题机械生成，至少包含用户任务和可验证动作。
- [x] 主流程可以从入口走到结果，不能出现死按钮或断头路。
- [x] 处理中、成功、失败、空状态、删除和重试等已声明状态有对应表现；未声明内容必须标注为假设。
- [x] 每个跳转目标真实存在，点击后页面标题和内容同步变化。
- [x] PRD 中的关键名词、业务对象和动作没有被泛化成“继续”“操作结果”等空文案。
- [x] 专业场景不会静默降级；降级时必须在 manifest 和交付说明中说明原因。
- [x] 刷新后编辑结果仍然存在，撤销/重做不会破坏页面状态。
- [x] 至少用一份未参与开发的 PRD 做盲测，检查生成者是否能复述需求意图。
- [x] 生成结果必须能被后续 Agent 继续修改，而不是只能人工改 HTML。

## 当前证据（2026-07-13）

- 39 项自动测试覆盖需求解析、通用业务对象、路由、专业交接、结果保真、安装包和质量基线。
- 跨领域基准覆盖内容审核、通知、门店排班和 AI 试穿策略；本地路径与专业路径均验证。
- 使用未参与实现的 `inputs/prd-original.md`（AI 试穿）实测：识别角色为 AI 试穿用户，目标为传统 tryon 链路改造；图片说明和飞书富文本不会再污染业务对象。
- 真实浏览器 13 项检查通过：页面/场景切换、直接编辑、撤销重做、Agent 反馈、导出、刷新持久化，以及专业结果的样式、动态 DOM 和编辑持久化。
- ZIP 从临时目录独立解压并运行成功，包根目录包含 `SKILL.md`。

## 尚未宣称的结论

- 自动检查证明统一入口与交付层可靠，不等于已证明每个外部专业 Skill 的生成质量。
- 与 78/100 竞品基线的最终同题横评，必须在专业 Skill 对真实 PRD 完成生成后，按 `references/capability-parity.json` 收集证据并复评分；缺证据时状态保持 `review-required`。

## 发布门槛

只有满足以下条件才允许宣称“可用”：

- 自动化测试全部通过。
- 至少两种不同领域 PRD 通过盲测。
- 至少一个复杂状态流通过人工对抗验收。
- 专业路由和本地兜底行为可解释、可追踪、可回滚。
